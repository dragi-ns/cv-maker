import { Component } from 'react';
import { CVForm } from './CVForm';

export default class Main extends Component {
  render() {
    return (
      <main>
        <CVForm />
      </main>
    );
  }
}
